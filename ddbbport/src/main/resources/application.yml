datasource:
  url: ${DATASOURCE_URL:jdbc:postgresql://localhost:5432/tiendaonline}
  username: ${DATASOURCE_USERNAME:postgres}
  password: ${DATASOURCE_PASSWORD:postgres}
  driver-class-name: org.postgresql.Driver